import { RemoveColonPipe } from './remove-colon.pipe';

describe('RemoveColonPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveColonPipe();
    expect(pipe).toBeTruthy();
  });
});
